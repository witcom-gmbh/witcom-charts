apiVersion: v1
kind: Service
metadata:
  name: {{ include "noc-api.fullname" . }}
  labels:
    {{- include "noc-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "noc-api.selectorLabels" . | nindent 4 }}
