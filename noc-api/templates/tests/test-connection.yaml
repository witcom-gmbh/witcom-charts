apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "noc-api.fullname" . }}-test-connection"
  labels:
    {{- include "noc-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "noc-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
