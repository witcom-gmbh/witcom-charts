apiVersion: v1
kind: Service
metadata:
  name: {{ include "extranet-docs-docservice.fullname" . }}
  labels:
    {{- include "extranet-docs-docservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "extranet-docs-docservice.selectorLabels" . | nindent 4 }}
